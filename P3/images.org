#+TITLE: P3/images

* cleanup
** Move images based on their rough aspect ratio into different directories.
#+BEGIN_SRC markdown
Move images based on their rough aspect ratio into different directories. Write a Python script to do this `images_group_by_aspect_ratio.py`.
#+END_SRC
